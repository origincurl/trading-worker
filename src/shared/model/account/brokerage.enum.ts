// Mirrors trading-be Brokerage enum. Drift between BE / worker MUST be
// caught in review — same row uses these strings on both sides.
export enum Brokerage {
  Kiwoom = 'KIWOOM',
  KoreaInvest = 'KOREA_INVEST',
}
